# TensorGym API + test dependencies (host side).
# The API process NEVER imports torch; PyTorch lives only in the executor image.
# Versions captured from the verified working environment on 2026-06-15.
fastapi==0.137.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
PyYAML==6.0.3
SQLAlchemy==2.0.51
pytest==9.1.0
httpx==0.28.1
